--- portal_src/game/board.h
+++ portal_src/game/board.h
@@ -744,2 +744,9 @@
         
+        if (pos.x() == 8 && pos.y() == 12 && dir == 0) {
+            std::cerr << "TRACE I13 H: key = " << std::hex << key << std::dec << " ";
+            for (int i = 0; i < 9; i++) {
+                if (i == 4) continue;
+                std::cerr << "P=" << windowCells[i].index() << " C=" << (windowCells[i] == Pos::PASS ? -1 : pieceAt(windowCells[i])) << " ";
+            }
+            std::cerr << std::endl;
+        }
         return key;
